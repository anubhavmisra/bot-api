var assert = require('should');
describe('select', function () {
  it('should add item to the basket', function () {
    //FIXME: Assert that the search results are valid
    var basketItems = [1];
    basketItems.length.should.aboveOrEqual(1);
  });
});